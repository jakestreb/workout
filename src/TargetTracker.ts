import WorkoutStage from './WorkoutStage';

// const MAX_LEFTOVER_TIME_S = 5 * 60;

export default class TargetTracker {
	constructor(targetName: string, intensity: number, timeMinutes: number) {

	}

	checkStages(stages: WorkoutStage[]) {
	    // // Check if limit has been surpassed
	    // const activations = exercise.activations;
	    // const newTargets = update(targets, activations);
	    // if (newTargets has improper nonzero values) {
	    //   return;
	    // }
	    // const totalIntensity = intensity + activations;
	    // if (intensity is too much) {
	    //   return;
	    // }
	    // yield new Workout();

	    // Always return true for every combo
	    return true;
	}
}
